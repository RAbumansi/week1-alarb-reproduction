datasets==4.0.0
pyarrow==25.0.1
pytest==9.1.1
numpy==2.5.3
scikit-learn==1.9.1

# Only needed for the default semantic-distractor embedder. Skip these and pass
# --embedder tfidf to build the MCQ tasks without torch.
sentence-transformers==6.1.0
torch==2.14.0
